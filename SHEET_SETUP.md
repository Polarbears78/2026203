# 설문 응답을 구글 시트에 자동 저장하기 (선생님용 설정)

설문(`survey.html`)에서 학생이 **제출하기**를 누르면, 답변이 구글 시트에 한 줄씩 자동으로 쌓이게 만드는 방법입니다. 무료이고 서버가 필요 없습니다. **딱 한 번만** 설정하면 됩니다.

## 1) 구글 시트 만들기
1. [sheets.new](https://sheets.new) 로 새 구글 시트를 만듭니다. (이름은 예: `학교생활 소감 설문 응답`)

## 2) Apps Script 붙여넣기
1. 시트 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 편집기의 기존 코드를 모두 지우고, 아래 `Code.gs` 내용을 붙여넣습니다. (이 저장소의 `Code.gs` 파일과 동일)
3. 💾 저장(Ctrl+S)

## 3) 웹 앱으로 배포
1. 오른쪽 위 **배포 → 새 배포**
2. 톱니바퀴(유형 선택) → **웹 앱**
3. 설정
   - 설명: 아무거나 (예: 설문 v1)
   - **다음 사용자로 실행: 나**
   - **액세스 권한이 있는 사용자: 모든 사용자**
4. **배포** → 권한 요청이 뜨면 본인 구글 계정으로 허용
5. 표시되는 **웹 앱 URL**을 복사합니다. (형식: `https://script.google.com/macros/s/……/exec`)

## 4) 설문에 URL 연결
- 이 URL을 저에게 알려주시면 `survey.html`의 `SHEET_ENDPOINT`에 넣어 배포해 드립니다.
- 직접 하실 경우: `survey.html` 상단의 `const SHEET_ENDPOINT = '';` 의 따옴표 안에 URL을 붙여넣고 저장하면 됩니다.

## 확인
- 설문 페이지에서 테스트로 제출 → 시트 `응답` 탭에 새 줄이 생기면 성공입니다.
- 열 순서: 제출시각 · 학번 · 이름 · 1인1역/직책 · 학교행사 · 학업역량 · 희망진로 · 진로노력 · 장점과단점 · 협력/갈등 · 성장포인트 · 기타

## 참고
- 브라우저 보안상 응답은 "쓰기 전용"으로 전송됩니다(제출 성공 여부만 표시). 실제 저장 내용은 시트에서 확인하세요.
- 코드를 수정했으면 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**로 재배포해야 반영됩니다.
