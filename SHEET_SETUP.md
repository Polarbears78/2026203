# 폼 응답을 구글 시트에 자동 저장하기 (선생님용 설정)

`survey.html`(학교생활 소감 설문)과 `study-log.html`(방학 학습 기록)에서 학생이 제출하면,
같은 구글 시트의 **서로 다른 탭**에 한 줄씩 자동으로 쌓입니다. 무료이고 서버가 필요 없습니다.

- 학교생활 소감 설문 → **`응답`** 탭
- 우리 반 게시판 → **`게시판`** 탭 (열: 작성시각·닉네임·내용) — 부적절한 글은 시트에서 행을 지워 관리하세요.
- 방학 학습 기록 → **`학습기록`** 탭 (열: 제출시각·날짜·번호·이름·국어·수학·영어·영어단어·**과학·독서활동**)
  - 과학·독서활동은 기존 데이터 정렬 보존을 위해 **맨 뒤(9·10열)** 에 추가됩니다. 최신 `Code.gs`로 **재배포**하면 헤더가 자동 확장됩니다.
- 수학여행 방배정 희망 조사 → **`방배정설문`** 탭 (열: 제출시각·번호·이름·성별·1순위·2순위·기피)
  - 기피 응답은 학생에게 공개되지 않습니다. 시트 접근 권한을 담임만 갖도록 관리하세요.

> **이미 배포하셨다면:** `Code.gs`를 최신 내용으로 다시 붙여넣고 **재배포(새 버전)** 만 하면
> 학습 기록 저장/통계 읽기 기능이 켜집니다. 웹 앱 URL은 그대로 유지됩니다. (재배포 방법은 아래 맨 끝 참고)

## 통계 페이지(`study-stats.html`)를 쓰려면
`study-stats.html`은 `학습기록` 탭 데이터를 읽어 번호별 합계 표로 보여줍니다.
이 읽기 기능은 최신 `Code.gs`의 `doGet(... action=studyStats ...)` 에 들어 있으므로,
**학습 기록을 저장하는 스크립트를 최신 `Code.gs`로 업데이트 후 재배포**하면 통계 페이지가 동작합니다.
(별도 인증 없이 데이터를 읽으므로, 통계 페이지 주소를 공유할 범위는 신중히 정하세요.)

## 방배정 페이지(`rooms.html`)를 쓰려면
1. 학생들에게 **`room-survey.html`**(홈 → 방배정 희망 조사) 링크를 안내해 응답을 받습니다.
2. 교사용 **`rooms.html`**(관리 콘솔 → 방배정)에서 **설문 결과 불러오기 → 방 배정하기**를 누릅니다.
   - 남녀를 따로 배정하며, **방 구성**은 남·여 각각 자유롭게 지정합니다.
     - `4` → 4명 기준 자동 분배 · `4,4,3,3` → 방마다 인원 직접 지정
     - `4*3, 3*2` → 4인실 3개 + 3인실 2개 · `401:4, 402:4` → 호실 이름까지 지정
     - 정원이 인원보다 많으면 뒷방부터 빈자리가 생기고, 모자라면 경고가 뜹니다.
   - 미응답 학생은 `번호, 이름, 성별` 형식으로 직접 추가하면 함께 배정됩니다.
   - 결과에서 학생을 **두 명 클릭**하면 자리를 서로 바꿀 수 있고, 표 복사·이미지 저장·인쇄가 가능합니다.
3. 이 읽기 기능은 최신 `Code.gs`의 `doGet(... action=roomSurvey ...)`에 있으므로 **재배포**가 필요합니다.

## 1) 구글 시트 만들기
1. [sheets.new](https://sheets.new) 로 새 구글 시트를 만듭니다. (이름은 예: `학교생활 소감 설문 응답`)

## 2) Apps Script 붙여넣기
1. 시트 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 편집기의 기존 코드를 모두 지우고, 아래 `Code.gs` 내용을 붙여넣습니다. (이 저장소의 `Code.gs` 파일과 동일)
3. 💾 저장(Ctrl+S)

## 3) 웹 앱으로 배포
1. 오른쪽 위 **배포 → 새 배포**
2. 톱니바퀴(유형 선택) → **웹 앱**
3. 설정
   - 설명: 아무거나 (예: 설문 v1)
   - **다음 사용자로 실행: 나**
   - **액세스 권한이 있는 사용자: 모든 사용자**
4. **배포** → 권한 요청이 뜨면 본인 구글 계정으로 허용
5. 표시되는 **웹 앱 URL**을 복사합니다. (형식: `https://script.google.com/macros/s/……/exec`)

## 4) 설문에 URL 연결
- 이 URL을 저에게 알려주시면 `survey.html`의 `SHEET_ENDPOINT`에 넣어 배포해 드립니다.
- 직접 하실 경우: `survey.html` 상단의 `const SHEET_ENDPOINT = '';` 의 따옴표 안에 URL을 붙여넣고 저장하면 됩니다.

## 확인
- 설문 페이지에서 테스트로 제출 → 시트 `응답` 탭에 새 줄이 생기면 성공입니다.
- 열 순서: 제출시각 · 학번 · 이름 · 1인1역/직책 · 학교행사 · 학업역량 · 희망진로 · 진로노력 · 장점과단점 · 협력/갈등 · 성장포인트 · 기타

## 콘텐츠 게시(주간안내·공지사항)를 쓰려면 — 관리 콘솔 연동
관리 콘솔(`content.html`)에서 편집한 **주간안내·공지사항**을 학생용 웹 앱에 게시하는 기능입니다.
게시된 내용은 시트의 **`콘텐츠`** 탭(열: kind·json·updated)에 저장되고, 학생용
`weekly.html`(주간안내)과 `index.html`(공지 카드)이 여기서 읽어 표시합니다.

설정(1회):
1. 최신 `Code.gs`를 붙여넣고 저장합니다.
2. Apps Script 왼쪽 **⚙️ 프로젝트 설정 → 스크립트 속성 → 속성 추가**
   - 속성: `ADMIN_KEY` / 값: 원하는 비밀번호 (예: 긴 무작위 문자열)
3. **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포** 로 재배포합니다.
4. 관리 콘솔 → 콘텐츠 페이지의 **관리자 키** 칸에 같은 비밀번호를 입력하면 게시됩니다.

> 관리자 키가 없으면 게시(쓰기)가 거부되므로 학생이 내용을 바꿀 수 없습니다.
> 읽기(action=content)는 인증 없이 공개됩니다 — 주간안내·공지 용도로만 사용하세요.

## 참고
- 브라우저 보안상 응답은 "쓰기 전용"으로 전송됩니다(제출 성공 여부만 표시). 실제 저장 내용은 시트에서 확인하세요.
- 코드를 수정했으면 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**로 재배포해야 반영됩니다.
